import { WorkspaceModuleSkeleton } from "@/components/loading/page-skeletons";

export default function TurnaroundTimeLoading() {
  return <WorkspaceModuleSkeleton statCount={4} leftPanelCount={1} rowCount={5} />;
}
